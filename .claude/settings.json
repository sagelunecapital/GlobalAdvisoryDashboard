{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git checkout.*--'; then git diff --quiet || (echo 'BLOCKED: uncommitted changes'; exit 1); fi"
          }
        ]
      }
    ]
  }
}

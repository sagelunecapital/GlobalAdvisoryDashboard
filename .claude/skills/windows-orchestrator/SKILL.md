# Windows Orchestrator Mode

We're on Windows. Skip the WSL daemon entirely.

Act as the Delivery Orchestrator directly in this session:
- Spawn Planning, Implementation, and QA sub-agents within this session
- Use PowerShell + GitHub API instead of gh CLI
- Use direct git operations instead of flock

Confirm this plan with the user before starting any delivery work.

# Resume Batch Changes

Resume pending changes from the prior session using prior context.

Rules:
- Start editing immediately — do not Read files speculatively
- Only Read a file if an Edit fails or the content is genuinely unknown
- Commit after each change so progress is preserved if the usage limit is hit
- State which change number you are on before each edit (e.g. "Change 3/8")

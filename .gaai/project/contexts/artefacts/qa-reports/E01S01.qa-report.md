---
type: artefact
artefact_type: qa-report
id: E01S01
verdict: PASS
track: delivery
skills_invoked:
  - .gaai/core/skills/delivery/qa-review/SKILL.md
  - .gaai/core/skills/cross/memory-alignment-check/SKILL.md
created_at: 2026-04-23
---

# QA Report — E01S01: Fetch Live Indicator Data

## Verdict: PASS

---

## Test Results

**Independent re-run:** 28 passed, 0 failed, 0 errors — 1.77s

```
tests/test_append.py            6 passed
tests/test_historical_load.py   5 passed
tests/test_live_fetch.py       11 passed  (new — E01S01 scope)
tests/test_schema.py            6 passed
```

Confirms the Implementation Sub-Agent's reported count of 28 tests with zero failures or errors. No tests were skipped or disabled.

---

## AC Validation

| AC | Status | Evidence |
|----|--------|----------|
| AC1: SPX daily high fetched | PASS | `fetch_live` reads `spx_df.iloc[-1]["spx_daily_high"]` from `fetch_spx()`. Column name matches DEC-2026-04-18-01. Tests T1 (key present, non-null) and T2 (value persisted to DB) confirm. |
| AC2: SPX 12-day EMA fetched | PASS | `fetch_live` reads `spx_df.iloc[-1]["spx_12d_ema"]`. Computed by `fetch_spx()` via `pandas.ewm(span=12, adjust=False).mean()` on full 2-year series (DEC-2026-04-22-01). Tests T1 and T2 confirm. |
| AC3: SPX 25-day EMA fetched | PASS | `fetch_live` reads `spx_df.iloc[-1]["spx_25d_ema"]`. Computed by `fetch_spx()` via `pandas.ewm(span=25, adjust=False).mean()`. Tests T1 and T2 confirm. |
| AC4: MMTH fetched via EODData | PASS | `fetch_live` calls `fetch_mmth()`. `fetch_mmth()` uses EODData as the active path (DEC-2026-04-22-02 — yfinance ^MMTH returns 404). Test T1 and T2 confirm value is returned and persisted. |
| AC5: Fetch failure → visible error naming indicator(s) | PASS | SPX and MMTH fetches are each wrapped in independent try/except blocks. Errors accumulate in `errors[]`. If non-empty, `FetchError` is raised naming "SPX (daily high, 12d EMA, 25d EMA)" or "MMTH" or both. `append_day()` is never called in this path. Tests T4 (SPX fail), T5 (MMTH fail), T6 (both fail) confirm. No bare `except: pass` exists — no silent crash. |
| AC6: Null/NaN value → fail, no partial data, no DB write | PASS | After fetches succeed, all 4 values are checked via `pd.isna()` before any DB write. If any null: `FetchError` raised before `append_day()`. No regime classification is possible from a `FetchError`. Tests T7–T10 each inject NaN for one field and assert count=0 in DB. Structurally identical failure path to AC5 from caller's perspective. |
| AC7: Successful fetch persisted (append-only) | PASS | `append_day()` called only after all 4 values pass null and finiteness checks. `append_day()` uses `INSERT OR IGNORE` — append-only, idempotent. No UPDATE or DELETE SQL in the call path. Test T2 verifies row persisted with matching values; T3 verifies idempotency (second call = still 1 row). |

---

## Rule Compliance

**Scope integrity:** Git commit `a33bcb0` shows exactly 2 files created: `src/fetch/live.py` (+122 lines) and `tests/test_live_fetch.py` (+160 lines). Zero pre-existing files modified. No scope drift.

**Decision compliance:**
- DEC-2026-04-18-01: `spx_daily_high` column name used throughout. `fetch_spx()` extracts `raw["High"]`. PASS.
- DEC-2026-04-22-01: Python + SQLite + yfinance + pandas stack used. EMA computation via `pandas.ewm(span=N, adjust=False).mean()` in `fetch_spx()`. PASS.
- DEC-2026-04-22-02: MMTH via `fetch_mmth()` which routes to EODData. PASS.

**Append-only enforcement:** DB writes delegated to `append_day()`. Confirmed `INSERT OR IGNORE` in `src/db/append.py` with no UPDATE or DELETE. Test `test_no_update_or_delete_sql_in_append` enforces this statically via source inspection.

**No architectural decisions made beyond story scope:** `fetch_live()` is a thin orchestrator. No new modules, services, or dependencies introduced beyond what was already in the codebase.

**Error handling quality:** Each external source isolated in its own try/except. No silent failures. `FetchError` is a `RuntimeError` subclass (T11 confirms). Error messages are descriptive and name the failing indicator.

**Dead code / unreachable branches:** None. The `_spx_df` and `_mmth_series` injection parameters are used in all 11 tests — fully exercised.

**Static analysis note:** Flake8 flags two unused imports (`numpy as np`, `pandas as pd`) and one line-length violation (1 char over) in `tests/test_live_fetch.py`. The project has no configured flake8 gate (`pytest.ini` disables pylint; no `.flake8`, `setup.cfg`, or `pyproject.toml` exists; `patterns/conventions.md` documents no lint gate). These issues are in test-only code and do not constitute a delivery gate failure. Logged in Friction Log below.

---

## Memory Alignment Check

Memory entries in scope for E01S01 (fetch layer, SPX, MMTH, DB persistence):

### Confirmed Entries

- **memory_id:** PROJECT-001
  **status:** CONFIRMED
  **suggested_last_verified_at:** 2026-04-23
  **suggested_verified_against_story:** E01S01
  **note:** Tech stack (Python, SQLite WAL, yfinance, pandas) and `spx_daily_high` convention correctly applied in `fetch_live()` and `fetch_spx()`. Module 1 (Market Regimes) data persistence pattern — append-only, no delete — implemented as specified.

- **memory_id:** PROJECT-002
  **status:** CONFIRMED
  **suggested_last_verified_at:** 2026-04-23
  **suggested_verified_against_story:** E01S01
  **note:** MMTH source (EODData) and SPX daily high convention both confirmed by the implementation. `fetch_mmth()` uses `eodhistoricaldata.com` API; `fetch_spx()` uses yfinance `^GSPC` `High` column.

- **memory_id:** DECISIONS-LOG (DEC-2026-04-18-01)
  **status:** CONFIRMED
  **suggested_last_verified_at:** 2026-04-23
  **suggested_verified_against_story:** E01S01
  **note:** `spx_daily_high` column name used. `fetch_spx()` extracts `raw["High"]`. Consistent with decision.

- **memory_id:** DECISIONS-LOG (DEC-2026-04-22-01)
  **status:** CONFIRMED
  **suggested_last_verified_at:** 2026-04-23
  **suggested_verified_against_story:** E01S01
  **note:** Python + SQLite + yfinance + pandas stack. EMA via `pandas.ewm(span=N, adjust=False).mean()`. Consistent with decision.

- **memory_id:** DECISIONS-LOG (DEC-2026-04-22-02)
  **status:** CONFIRMED
  **suggested_last_verified_at:** 2026-04-23
  **suggested_verified_against_story:** E01S01
  **note:** EODData is the active MMTH source. `fetch_mmth()` routes through yfinance (always fails) then EODData fallback. Consistent with decision.

### Contradicted Entries

None.

### New Knowledge Candidates

See `E01S01.memory-delta.md`.

---

## Friction Log

1. **Unused imports in test file:** `tests/test_live_fetch.py` imports `numpy as np` and `pandas as pd` (lines 4–5) but neither is used directly — they are used in `conftest.py` fixtures. Flake8 F401. No gate impact (no flake8 gate configured). Remediation: remove unused imports in a future cleanup pass. No delivery block.

2. **Line length violation:** `tests/test_live_fetch.py` line 54 is 121 chars (1 over flake8 default). No project-configured max-line-length. No gate impact. Remediation: cosmetic wrap in future cleanup.

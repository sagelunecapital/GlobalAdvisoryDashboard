# Personal files — not part of the GAAI project
Journal/
Sector Rotation/
GAAI-framework-main/
*.html
!prototypes/index.html
!prototypes/mosaic-sample.html
*.docx
*.xlsx
*.twbx

# Delivery logs and worktrees — runtime artefacts, not committed
.gaai/project/contexts/backlog/.delivery-logs/
.gaai/project/contexts/backlog/.delivery-daemon.log
.gaai/worktrees/

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
*.egg-info/

# GAAI daemon — local runtime state, not committed
data/gaai-jobs.db
data/gaai-deliver.log

# Playwright — test runner artifacts
node_modules/
playwright-report/
test-results/
tests/ui/screenshots/

# Pipeline runtime caches / regenerated data — rebuilt each run, not committed
# (Sector Rotation/ is ignored above; these are the same kind of artifact)
scripts/barchart_zq_cache.json
data/cftc_cot.db
